import 'package:flutter/material.dart';

/// Root navigator key, used to navigate from outside the widget tree
/// (e.g. when a push notification is tapped).
final navigatorKey = GlobalKey<NavigatorState>();
